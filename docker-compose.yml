version: '3.8'

services:
  beatmaps:
    build: .
    container_name: beatmaps-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - TZ=UTC
    labels:
      - "com.beatmaps.description=BeatMaps - Rhythmic GPS Navigation"
